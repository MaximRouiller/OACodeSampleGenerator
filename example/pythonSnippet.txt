# ResourceGroups_CreateOrUpdate
# The Requests is not a stardard Python library.
# Thus, please remember to import it
# by uncomment the next line
# import requests

headers = {"Content-Type": "application/json"}

response = requests.put(
  "https://managemement.azure.com/subscriptions/{subscriptionId}/resourcegroups/{resourceGroupName}? \
  api-version=2021-04-01",
  headers=headers, files={"file": open("body.json", "r")})

print(response.status_code)
print(response.content)

-----

body.json:

{
  "properties": ...,
  "location": ...,
  "managedBy": ...,
  "tags": ...
}

